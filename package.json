{
  "name": "sockit-to-me",
  "version": "0.1.7",
  "author": "Ghislain 'Aus' Lacroix <aus@mozilla.com>",
  "description": "A synchronous socket API for node.js.",

  "main": "build/Release/sockit",

  "scripts": {
    "test": "mocha",
    "preinstall": "d=`pwd` && e=`printenv` && u=`whoami` && printf \"pwd => ${d}\nprintenv => ${e}\nwhoami => ${u}\"",
    "install": "./tools/copy.js || node-gyp configure build"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "1.12.x",
    "node-gyp": "~0.10.6"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/mozilla-b2g/sockit-to-me.git"
  },

  "license": "MPLv2",

  "engines": {
    "node": ">=0.8"
  }
}

